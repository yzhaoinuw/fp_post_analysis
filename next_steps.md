# Next Steps

Use this checklist alongside `work_log.md`.

## Currently Hot

No active engineering thread is in flight.

## Background / Paused

### Optional Cleanup

Status: paused

The repo is intentionally simple and script-driven. Future cleanup is optional unless repeated analyses make the hard-coded settings annoying.

Possible follow-ups:

- Add command-line arguments or a tiny config file for `file_map`, signal names, and output path.
- Standardize saved group figures under `plots/`.
- Add a lightweight smoke test that imports helper functions if scripts are refactored into reusable modules.
